/**
 * Created by on 11/8/17.
 */
const assert = require("assert")
const token = require("../../routes/token")

describe("Token test", () => {
    it("Test verify user", () => {
        assert(1 === 1)
    })
})